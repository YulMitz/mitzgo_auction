# Postgres — used by backend, postgres service, and any psql client
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=mitzgo
POSTGRES_PASSWORD=changeme
POSTGRES_DB=mitzgo_auction

# Backend — HTTP port the Express app listens on (inside the container)
BACKEND_PORT=3000
